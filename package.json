{
  "name": "async-queue",
  "version": "0.1.0",
  "description": "async queue implementation in Typescript",
  "main": "build/src/index.js",
  "types": "build/src/index.d.ts",
  "files": [
    "build/src"
  ],
  "license": "Apache-2.0",
  "keywords": [],
  "scripts": {
    "start": "ts-node src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm.cmd run compile",
    "pretest": "npm.cmd run compile",
    "posttest": "npm.cmd run check"
  },
  "devDependencies": {
    "gts": "^2.0.2",
    "typescript": "^3.8.3",
    "@types/node": "^13.11.1"
  }
}
